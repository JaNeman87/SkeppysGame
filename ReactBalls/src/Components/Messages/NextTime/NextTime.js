import React from "react";
import classes from "./NextTime.css";

const nextTime = props => {
  return (
    <div className={classes.Next}>
      <h2>Better Luck Next Time!</h2>;
    </div>
  );
};

export default nextTime;
